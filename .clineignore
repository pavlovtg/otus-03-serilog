# Бенчмарк-отчёты — не релевантны для задач агента, экономия токенов контекста.
# Читать только по явному запросу пользователя.
results/

# Бинарные ресурсы — иконки и ключ подписи сборки.
# Читать только по явному запросу пользователя.
assets/

# Артефакты сборки .NET-проектов — не несут смысловой нагрузки для анализа кода.
**/bin/
**/obj/

# Бинарные файлы — не поддаются текстовому анализу.
*.snk
*.png
*.svg
